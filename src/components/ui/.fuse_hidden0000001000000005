import type { CSSProperties, ReactNode } from "react";
import type { Meta, StoryObj } from "@storybook/nextjs-vite";

import { Ring } from "@/components/ui/ring";

const meta = {
  component: Ring,
  parameters: {
    docs: {
      description: {
        component:
          "도넛 링 데이터 신호. Project Time / Todo Progress / Timer / Activity Summary에 사용. track은 옅은 rain, stroke는 Sky Blue → Opal Lilac. 세그먼트는 프로젝트별 귀속(집중/딴짓 아님). 색은 Sky/Bubble Blue/Opal Lilac/Soft Pink로 제한, 청록 금지.",
      },
    },
  },
  title: "UI/Ring",
} satisfies Meta<typeof Ring>;

export default meta;

type Story = StoryObj<typeof meta>;

const row: CSSProperties = { display: "flex", gap: 28, alignItems: "center", flexWrap: "wrap" };

const legendItem: CSSProperties = { display: "flex", alignItems: "center", gap: 7, fontSize: 12, color: "var(--ink-soft)" };
function Dot({ c }: { c: string }) {
  return <span style={{ width: 8, height: 8, borderRadius: "50%", background: c, flex: "0 0 auto" }} />;
}

export const Default: Story = {
  render: () => (
    <div style={row}>
      <Ring label="진행" metric="64%" value={64} />
    </div>
  ),
};

// 프로젝트 귀속 시간 (segmented + legend)
export const ProjectTime: Story = {
  render: () => (
    <div style={row}>
      <Ring
        label="오늘 기록"
        metric="2h15m"
        segments={[
          { color: "#6FB8F2", label: "ClientA", value: 86 },
          { color: "#B0A8E0", label: "자료 정리", value: 32 },
          { color: "#CBD8DC", label: "미분류", value: 17 },
        ]}
        size={120}
        thickness={11}
      />
      <div style={{ display: "grid", gap: 5 }}>
        <span style={legendItem}>
          <Dot c="#6FB8F2" />
          ClientA <b style={{ marginLeft: "auto" }}>1h26</b>
        </span>
        <span style={legendItem}>
          <Dot c="#B0A8E0" />
          자료 정리 <b style={{ marginLeft: "auto" }}>32m</b>
        </span>
        <span style={legendItem}>
          <Dot c="#CBD8DC" />
          미분류 <b style={{ marginLeft: "auto" }}>17m</b>
        </span>
      </div>
    </div>
  ),
};

export const Segmented: Story = {
  render: () => (
    <div style={row}>
      <Ring
        label="비율"
        metric="100%"
        segments={[
          { color: "#6FB8F2", value: 4 },
          { color: "#B0A8E0", value: 3 },
          { color: "#F2BBD2", value: 2 },
          { color: "#9ED8FF", value: 2 },
        ]}
      />
    </div>
  ),
};

export const TodoProgress: Story = {
  render: () => (
    <div style={row}>
      <Ring label="오늘 할 일" metric="3/6" value={50} variant="todo" />
    </div>
  ),
};

export const Timer: Story = {
  render: () => (
    <div style={row}>
      <Ring label="집중" metric="25:00" value={42} variant="timer" />
    </div>
  ),
};

export const Empty: Story = {
  render: () => (
    <div style={row}>
      <Ring label="기록 없음" metric="0m" value={0} />
    </div>
  ),
};

export const Complete: Story = {
  render: () => (
    <div style={row}>
      <Ring label="완료" metric="100%" value={100} />
    </div>
  ),
};

function DarkFrame({ children }: { children: ReactNode }) {
  return (
    <div data-theme="dark" style={{ background: "#161E2E", borderRadius: 20, padding: 28 }}>
      {children}
    </div>
  );
}

export const Dark: Story = {
  parameters: { backgrounds: { default: "dark" } },
  render: () => (
    <DarkFrame>
      <div style={row}>
        <Ring label="오늘 기록" metric="2h15m" segments={[{ color: "#6FB8F2", value: 86 }, { color: "#B0A8E0", value: 32 }, { color: "#3B4A66", value: 17 }]} size={110} thickness={11} />
        <Ring label="집중" metric="25:00" value={42} />
      </div>
    </DarkFrame>
  ),
};
