import type { CSSProperties, ReactNode } from "react";
import type { Meta, StoryObj } from "@storybook/nextjs-vite";

import { ProgressBar } from "@/components/ui/progress-bar";

const meta = {
  args: { value: 60 },
  component: ProgressBar,
  parameters: {
    docs: {
      description: {
        component:
          "진행률 데이터 신호. track은 옅은 rain/mist, progress는 Sky Blue → Opal Lilac. 완료는 작은 soft green으로 마무리. 청록/민트/네온 금지.",
      },
    },
  },
  title: "UI/ProgressBar",
} satisfies Meta<typeof ProgressBar>;

export default meta;

type Story = StoryObj<typeof meta>;

const wrap: CSSProperties = { display: "grid", gap: 16, maxWidth: 360 };

export const Default: Story = {
  render: () => (
    <div style={wrap}>
      <ProgressBar value={60} />
    </div>
  ),
};

export const WithLabel: Story = {
  render: () => (
    <div style={wrap}>
      <ProgressBar label="랜딩 프로젝트" showValue value={68} />
    </div>
  ),
};

export const Low: Story = {
  render: () => (
    <div style={wrap}>
      <ProgressBar label="자료 정리" showValue value={18} />
    </div>
  ),
};

export const Medium: Story = {
  render: () => (
    <div style={wrap}>
      <ProgressBar label="에이전트 고도화" showValue value={52} />
    </div>
  ),
};

export const High: Story = {
  render: () => (
    <div style={wrap}>
      <ProgressBar label="랜딩 프로젝트" showValue value={86} />
    </div>
  ),
};

export const Complete: Story = {
  render: () => (
    <div style={wrap}>
      <ProgressBar label="시안 1차" showValue value={100} />
    </div>
  ),
};

export const Loading: Story = {
  render: () => (
    <div style={wrap}>
      <ProgressBar indeterminate label="자료 분석 중" showValue value={0} />
    </div>
  ),
};

function DarkFrame({ children }: { children: ReactNode }) {
  return (
    <div data-theme="dark" style={{ background: "#161E2E", borderRadius: 20, padding: 28, maxWidth: 400 }}>
      {children}
    </div>
  );
}

export const Dark: Story = {
  parameters: { backgrounds: { default: "dark" } },
  render: () => (
    <DarkFrame>
      <div style={{ display: "grid", gap: 16 }}>
        <ProgressBar label="랜딩 프로젝트" showValue value={68} />
        <ProgressBar indeterminate label="자료 분석 중" showValue value={0} />
        <ProgressBar label="시안 1차" showValue value={100} />
      </div>
    </DarkFrame>
  ),
};
